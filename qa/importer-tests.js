const assert=require('assert');
const {fundedNext,mt5}=require('../lib/importers');
function state(){return {accounts:[{id:'a'}],trades:[],openPositions:[],importHistory:[]}}
function testFundedNext(){const csv=`Ticket ID,Open Time,Open Price,Close Time,Close Price,Profit,Lots,Commission,Swap,Symbol,Type,SL,TP,Pips,Volume\n1,2026.07.21 10:00:00,100,2026-07-21 11:00:00,110,10,0.1,-1,-0.5,XAUUSD,Buy,95,120,1000,10\n2,2026.07.21 10:00:00,100,Currently Running,101,-1,0.1,0,0,EURUSD,Sell,105,90,-10,10`;let d=state(),r=fundedNext(d,csv,'a','test.csv');assert.equal(r.added,1);assert.equal(r.diagnostics.openPositions,1);assert.equal(d.trades.length,1);assert.equal(d.openPositions.length,1);assert.equal(d.trades[0].netProfit,8.5);r=fundedNext(d,csv,'a','test.csv');assert.equal(r.diagnostics.duplicates,1);assert.equal(d.trades.length,1);}
function testMT5CSV(){const csv=`Ticket,Open Time,Open Price,Close Time,Close Price,Profit,Volume,Commission,Swap,Symbol,Type,SL,TP\n100,2026-07-21 10:00,1.1,2026-07-21 11:00,1.2,20,0.1,-1,0,EURUSD,buy,1.0,1.3`;let d=state(),r=mt5(d,csv,'a','report.csv',false);assert.equal(r.added,1);assert.equal(r.diagnostics.format,'csv');assert.equal(d.trades[0].netProfit,19);assert.equal(d.trades[0].side,'Buy');}
function testMT5HTML(){const html='<table><tr><th>Ticket</th><th>Open Time</th><th>Close Time</th><th>Profit</th><th>Symbol</th><th>Type</th></tr><tr><td>200</td><td>2026-07-21 10:00</td><td>2026-07-21 11:00</td><td>-5</td><td>US30</td><td>sell</td></tr></table>';let d=state(),r=mt5(d,html,'a','report.html',true);assert.equal(r.added,1);assert.equal(r.diagnostics.format,'html');assert.equal(d.trades[0].symbol,'US30');assert.equal(d.trades[0].side,'Sell');}
testFundedNext();testMT5CSV();testMT5HTML();console.log('Importer QA: PASS (FundedNext, MT5 CSV, MT5 HTML, duplicates, open positions)');
